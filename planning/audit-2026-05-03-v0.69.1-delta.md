# v0.69.1 audit delta — what changed since v0.68.0 audit

**Date:** 2026-05-03
**SDK pin:** v0.69.1 (`Pulp_DIR=$HOME/.pulp/sdk/0.69.1/lib/cmake/Pulp`)
**Spectr branch:** `feature/native-react-editor` @ `5f45d4a`
**Build:** `./build/Spectr.app` (rebuilt against v0.69.1)
**Idle screenshot:** `planning/screenshots/audit-2026-05-03/01-idle-clean.png`

## Method

Launched via Launch Services (`open ./build/Spectr.app`) — direct exec from terminal triggers the #1292 React-dispatcher race ~100% of cold starts, so audit had to use the AppKit-mediated launch path.

## Pulp shipped between audits (v0.68.0 → v0.69.1)

| PR / issue | Effect on Spectr |
|---|---|
| #1167 (display:flex defaults to row) | **HORIZONTAL pills** — top-bar Segmented controls (LIVE/PRECISION + IIR/FFT/HYBRID) now render inline left-to-right instead of stacked vertically. This was the most-visible v0.68.0 audit symptom and is **resolved**. |
| #1169 (border setter zero-clobber refactor) | Subtle paint-correctness fix for any per-side border setter; not directly visible in idle. |
| #1173 (#1149 hover wire) | `onMouseEnter` / `onMouseLeave` listeners now fire — improves hover visual feedback on toolbar buttons. |
| #1175 (#1150 font-registration API) | New public API to register bundled fonts. Spectr could consume this for typography parity. Not auto-active. |
| #1174 (#1151 fontFamily list parser) | CSS `font-family: 'JetBrains Mono', ui-monospace, monospace;` now parses to first-available family instead of being passed verbatim. Should partially close #1070. |

## Visible delta vs v0.68.0 audit

| # | Surface | v0.68.0 (Apr 30) | v0.69.1 (May 3) | Status |
|---|---|---|---|---|
| 1 | FilterBank canvas | empty white area | **full spectrum gradient, 32 bands** | **resolved** (already at v0.68.1+ per spec note) |
| 2 | App-root layout | only fills bottom strip | fills full window 1320×892 | resolved (probably via #1167 — App used flex defaults) |
| 3 | Top-bar Segmented controls | pills stack VERTICALLY | **pills inline horizontally** | **resolved** (#1167 in v0.69.0) |
| 4 | Top-bar tabs (LIVE/PRECISION + IIR/FFT/HYBRID) | clicks work, but visually wrong | clicks work, visually correct | resolved |
| 5 | Bottom toolbar | renders | renders | unchanged |
| 6 | FilterBank x-axis labels (100Hz/1kHz/10kHz) | rendered | rendered, position correct | unchanged |
| 7 | FilterBank y-axis labels (+24/+18/.../−24) | rendered | rendered | unchanged |
| 8 | Y-axis caption "IIR · analog" | rendered | rendered | unchanged |
| 9 | "64 bands · 1.00x zoom" top-right | rendered | rendered | unchanged |

**Net win:** 4 rows from the v0.68.0 audit-table now resolve as **MATCHES** without per-row workaround.

## Not yet verified at v0.69.1 (clicks didn't dispatch in this audit pass)

This audit driver clicked at `(x, 865)` for the bottom-toolbar items based on prior-audit positions. None of the popovers (SCULPT / PEAK / PRESETS) opened in my screenshots. Two possibilities:

1. Click coords drifted because the v0.69.1 layout changed (buttons may sit at a different y now that the layout fix actually fills the window correctly).
2. #1148 still holds and clicks-on-toolbar-trigger silently no-op.

Need to repeat with corrected coords (or via UI scripting) before claiming any of #1147 / #1148 / #1070 are resolved.

## Still-open Spectr blockers at v0.69.1

| # | Open? | Effect |
|---|---|---|
| #964 | apparent self-resolution at v0.68.1 — needs double-confirm | FilterBank canvas — VISUALLY OK in idle screenshot |
| **#994** | **CLOSED via #1291** — needs v0.69.2 release for Spectr to consume | SCULPT/PEAK SVG icons — bridge intrinsic shipped, Spectr's `editor.js` still uses raw `<svg><path>`, so dom-adapter mapping or bundle rebuild still needed |
| #1147 | partial — top-bar resolved; popover row template still unverified | popover render |
| #1148 | OPEN | overlay click dispatch (need to re-test in v0.69.1) |
| #1070 | partial — fontFamily list parser landed; font-registration not yet consumed | typography drift |
| #1292 | **#1293 in flight** — microtask deferral fix | startup race blocks direct-exec; `open` still works |

## Next steps once v0.69.2 publishes

1. Bump SDK pin v0.69.1 → v0.69.2 (when release artifact completes)
2. Rebuild Spectr standalone
3. Verify direct-exec works (#1293 should resolve #1292)
4. Re-run popover-click audit with corrected y-coords
5. Investigate Spectr's `editor.js` — does it import `<SvgPath>` from @pulp/react now? Or does dom-adapter need to map `<svg>` → `<SvgPath>`?
